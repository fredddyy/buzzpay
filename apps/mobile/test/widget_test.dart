import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Full widget tests will be added in Phase 2
    expect(true, isTrue);
  });
}
