import 'package:flutter/material.dart';

class VColors {
  VColors._();

  // Dark-first — matches admin dashboard
  static const base = Color(0xFF0F1011);
  static const surface = Color(0xFF161819);
  static const surfaceLight = Color(0xFF1E2023);
  static const surfaceHover = Color(0xFF282B2E);
  static const border = Color(0xFF2A2D31);

  static const text = Color(0xFFE8E8E8);
  static const textSecondary = Color(0xFF9CA3AF);
  static const textMuted = Color(0xFF6B7280);

  static const primary = Color(0xFF6C4FFF);
  static const primarySurface = Color(0x146C4FFF);

  static const success = Color(0xFF22C55E);
  static const successSurface = Color(0x1422C55E);

  static const warning = Color(0xFFF59E0B);
  static const warningSurface = Color(0x14F59E0B);

  static const error = Color(0xFFEF4444);
  static const errorSurface = Color(0x14EF4444);

  static const info = Color(0xFF3B82F6);
}
